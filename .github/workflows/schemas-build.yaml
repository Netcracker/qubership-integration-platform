name: Schemas Build

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'schemas/**'

permissions: {}

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_maven-module-build.yaml
    with:
      module: schemas

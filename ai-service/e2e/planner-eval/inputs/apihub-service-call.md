Binding resolution policy: APIHUB_REQUIRED
API release: 2024.4

Create an HTTP POST `/orders` flow that invokes `Orders Service.createOrder`. Resolve the API and
operation from APIHub before generating the service call.

Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Create one chain with an HTTP GET `/greetings` trigger and an hourly quartz-scheduler trigger.
Both triggers connect independently to the same script. Do not connect either trigger to the
other trigger.

Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Create an HTTP GET `/safe-echo` flow with `try-catch-finally-2`. Put one success script under
`try-2` and one error script under `catch-2`. Preserve the required parent and priority
relationships.

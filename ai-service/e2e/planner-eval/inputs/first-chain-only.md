Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Integration flow for CIP Chain: Primary Orders
Create an HTTP GET `/primary` trigger followed by a response script.

Integration flow for CIP Chain: Secondary Audit
Create an HTTP GET `/secondary` trigger followed by a Kafka publisher.

Plan only the first integration flow.

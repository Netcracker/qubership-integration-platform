Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Create an HTTP POST `/customers` flow using the existing `Customer Service.createCustomer`
binding. Map `request.name` to `customer.fullName`. The mapping intent identifier is
`customer-map`.

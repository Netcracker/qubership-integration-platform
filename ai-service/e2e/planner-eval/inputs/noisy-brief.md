Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Historical notes mention Kafka, a loop, and an obsolete Orders service call. They are explicitly
out of scope. The accepted flow contains only an HTTP POST `/normalize` trigger and one script
that normalizes the request body before returning it.

Binding resolution policy: CATALOG_ONLY
Language version: 2024.4

Create one chain with an HTTP GET `/greetings` trigger followed by a script that returns plain
text. The flow has no service calls, mapping, retries, or error handling.

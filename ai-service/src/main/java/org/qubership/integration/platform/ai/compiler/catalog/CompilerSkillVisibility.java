package org.qubership.integration.platform.ai.compiler.catalog;

/** Visibility derived from compiler package metadata and private markers. */
public enum CompilerSkillVisibility {
  PUBLIC,
  PRIVATE,
  UNKNOWN
}

package org.qubership.integration.platform.ai.productpipeline.create.design.semantic;

/** Role of a condition branch. Deprecated choice/when/otherwise is not a role. */
public enum ConditionBranchRole {
  IF,
  ELSE
}

package org.qubership.integration.platform.ai.productpipeline.packageindex;

/** Provenance disposition of one selected target dependency against the reference baseline. */
public enum ReferenceDisposition {
  ADOPTED,
  ADAPTED,
  TARGET_ONLY
}

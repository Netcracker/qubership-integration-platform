# Untracked local override: apply .env.local AFTER ai-service-dev.env so real
# LLM credentials win over placeholders. Do not commit.
services:
  qip-ai-service:
    env_file:
      - ai-service-dev.env
      - .env.local
  qip-e2e-evaluator:
    env_file:
      - ai-service-dev.env
      - .env.local

apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}-ai-service
  name: {{ .Release.Name }}-ai-service
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}-ai-service

postgres:
  username: postgres
  password: postgres

global:
  postgres:
    database: postgres
  qip:
    ui:
      port: 30080
    variables:
      defaultSecret:
        enabled: false
    deploy:
      classic:
        enabled: true
      micro:
        enabled: true
        image: ghcr.io/netcracker/qubership-integration-micro-engine:main

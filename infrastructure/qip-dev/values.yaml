postgres:
  username: postgres
  password: postgres

global:
  blueGreenVersion: v1
  postgres:
    database: postgres
  qip:
    controlPlane:
      enabled: true
      meshType: Istio
    ui:
      port: 30080
    variables:
      defaultSecret:
        enabled: false
    deploy:
      classic:
        enabled: true
      micro:
        enabled: true
        image: ghcr.io/netcracker/qubership-integration-micro-engine:latest

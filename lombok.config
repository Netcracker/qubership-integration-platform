# `logstash-logback-encoder` brings Jackson 3 onto the classpath alongside Jackson 2, and @Jacksonized then has
# two variants to choose from. Every DTO here is annotated with `com.fasterxml.jackson`, so pin the generator to 2.
config.stopBubbling = true
lombok.jacksonized.jacksonVersions = JACKSON2

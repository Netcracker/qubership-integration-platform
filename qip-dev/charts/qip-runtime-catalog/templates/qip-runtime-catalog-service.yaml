apiVersion: v1
kind: Service
metadata:
  labels:
    app: qip-runtime-catalog
  name: qip-runtime-catalog
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    app: qip-runtime-catalog
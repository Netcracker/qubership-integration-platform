import type { CSSProperties } from "react";

// Shared weight for entity-name links in list grids (chains, services).
// Link color comes from the `.ant-table a` override, so only weight lives here.
export const nameLinkStyle: CSSProperties = { fontWeight: 500 };
